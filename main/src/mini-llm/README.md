* This is a full GPT impl almost replicating GPT2, architecture wise
* But instead of traning on tokens, we just train on chars
* Explore attentiin mechanisim
* Full traning pipeline, cosine scheduled LR
